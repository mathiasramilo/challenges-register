import { Manrope, Poppins, Ubuntu } from "next/font/google";

export const manrope = Manrope({
  subsets: ["latin"],
  display: "swap",
});
export const poppins = Poppins({
  weight: ["500", "600"],
  subsets: ["latin"],
  display: "swap",
});
export const ubuntu = Ubuntu({
  weight: ["500"],
  subsets: ["latin"],
  display: "swap",
});
